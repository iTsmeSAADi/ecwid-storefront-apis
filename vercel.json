{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.js"
        }
    ],
    "env": {
        "PUPPETEER_SKIP_DOWNLOAD": "false",
        "PUPPETEER_EXECUTABLE_PATH": "/usr/bin/chromium-browser"
    }
}
